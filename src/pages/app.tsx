import * as React from "react";
import { NextFunctionComponent } from "next";
import RootApp from "../app/App";

const RootAppComponent: NextFunctionComponent = () => {
  return <RootApp />;
};

export default RootAppComponent;
